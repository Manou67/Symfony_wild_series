{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Commentaires</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>Evaluation</th>
                <td>{{ comment.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a style="margin-left:10px;" href="{{ path('comment_index') }}">Retour à la liste</a>
<br>
    <a style="margin-left:10px;" href="{{ path('comment_edit', {'id': comment.id}) }}">Editer</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
